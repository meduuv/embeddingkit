# embeddingkit

Small dependency-free helpers for normalizing vectors and calculating cosine similarity.

```python
from embeddingkit import normalize, cosine
```

Run `pytest` to test. License: MIT.

Built by meduuv. `guns.lol/meduu`
