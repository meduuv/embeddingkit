[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "embeddingkit"
version = "0.1.0"
description = "Dependency-free vector utilities"
requires-python = ">=3.10"

[tool.hatch.build.targets.wheel]
packages = ["src/embeddingkit"]
